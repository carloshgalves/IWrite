<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.4.1</version>
        <relativePath/>
    </parent>

    <groupId>com.iwrite</groupId>
    <artifactId>iwrite-backend</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>iwrite-backend</name>
    <description>Backend foundation for IWrite MVP 1</description>

    <properties>
        <java.version>21</java.version>
        <jacoco.version>0.8.12</jacoco.version>
        <spring-ai.version>1.0.9</spring-ai.version>
        <!--
          Fixada em 1.64.0 porque é a versão do opentelemetry-java importada pelo
          opentelemetry-instrumentation-bom 2.30.0, que é a release do Java Agent
          embarcado no Dockerfile. Sobrepõe o 1.43.0 gerenciado pelo Spring Boot
          3.4.1. Ver docs/otel-business-signals.md#dependencias.
        -->
        <opentelemetry.version>1.64.0</opentelemetry.version>
        <!--
          Patch releases dentro da mesma linha gerenciada pelo Spring Boot 3.4.1
          (10.1.x), para corrigir CVEs CRITICAL do container-security.yml
          (Slice 80B/#80): CVE-2025-24813, CVE-2026-41293, CVE-2026-43512,
          CVE-2026-43515. Sobrepõe o 10.1.34 gerenciado pelo Spring Boot.
        -->
        <tomcat.version>10.1.59</tomcat.version>
        <!--
          Patch release dentro da mesma linha gerenciada pelo Spring Boot 3.4.1
          (6.4.x), para corrigir a CVE CRITICAL CVE-2025-41232
          (spring-security-core) do container-security.yml (Slice 80B/#80).
          Sobrepõe o 6.4.2 gerenciado pelo Spring Boot. Este patch torna
          BCryptPasswordEncoder.encode() estrito para senhas > 72 bytes, o que
          exigiu ajustar o fixture de teste legado de com.iwrite.auth.
          CredentialProvisioningRunnerIntegrationTest para semear um hash bcrypt
          precomputado em vez de hashear uma senha oversized ao vivo (nenhuma
          lógica de produção mudou). NÃO corrige CVE-2026-22732
          (spring-security-web): o advisory oficial cobre toda a linha
          6.4.0–6.4.14, o fix 6.4.15 é Enterprise-only, e o fix OSS só existe a
          partir de 6.5.9 — fora da linha 6.4.x que o Spring Boot 3.4.1 gerencia.
          Ver .trivyignore.yaml e docs/supply-chain-security.md, seção
          "Exceções de vulnerabilidade documentadas".
        -->
        <spring-security.version>6.4.13</spring-security.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-aop</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-database-postgresql</artifactId>
        </dependency>
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.apache.poi</groupId>
            <artifactId>poi-ooxml</artifactId>
            <version>5.5.1</version>
        </dependency>
        <!--
          Somente a API. O SDK de produção vem do Java Agent (Dockerfile); sem
          agente, GlobalOpenTelemetry.get() devolve a implementação no-op e a
          instrumentação manual não faz nada. Versão gerenciada pela propriedade
          opentelemetry.version acima.
        -->
        <dependency>
            <groupId>io.opentelemetry</groupId>
            <artifactId>opentelemetry-api</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- SDK + exporters em memória apenas para validar spans e métricas nos testes. -->
        <dependency>
            <groupId>io.opentelemetry</groupId>
            <artifactId>opentelemetry-sdk-testing</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.ai</groupId>
            <artifactId>spring-ai-starter-model-openai</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.ai</groupId>
            <artifactId>spring-ai-starter-model-anthropic</artifactId>
        </dependency>
        <!-- branch feature/umami-mcp (#128): única adição desta frente ao pom -->
        <dependency>
            <groupId>org.springframework.ai</groupId>
            <artifactId>spring-ai-starter-mcp-server-webmvc</artifactId>
        </dependency>
    </dependencies>

    <dependencyManagement>
        <dependencies>
                <dependency>
                <groupId>org.springframework.ai</groupId>
                <artifactId>spring-ai-bom</artifactId>
                <version>${spring-ai.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>${jacoco.version}</version>
                <executions>
                    <execution>
                        <id>prepare-agent</id>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>report</id>
                        <phase>test</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
